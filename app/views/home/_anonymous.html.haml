- if @auth.nil?
  =link_to('Log In', '/login', :remote => true)
  =link_to('Sign Up', new_user_path, :remote => true)
- else
  =link_to('New Bank', new_bank_path, :remote => true, :class => 'button tiny raius success')
